<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Résultat Conversion</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .container { max-width: 600px; margin: 0 auto; }
        .success { background: #d4edda; padding: 20px; border-radius: 5px; }
        .error { background: #f8d7da; padding: 20px; border-radius: 5px; }
        .result-table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        .result-table th, .result-table td { padding: 10px; border: 1px solid #ddd; text-align: left; }
        .result-table th { background-color: #f8f9fa; }
        pre { background: #f8f9fa; padding: 15px; border-radius: 5px; overflow-x: auto; }
    </style>
</head>
<body>
    <div class="container">
        <h1>💱 Résultat de Conversion</h1>
        
        <h2>Demande: {{ amount }} {{ from }} → {{ to }}</h2>
        
        {% if success %}
            <div class="success">
                <h3>✅ Conversion réussie</h3>
                
                {% if result.originalValues is defined %}
                    <table class="result-table">
                        <tr>
                            <th>Montant original</th>
                            <td>{{ result.originalValues.fromAmount }} {{ result.originalValues.fromCurrency }}</td>
                        </tr>
                        <tr>
                            <th>Montant converti</th>
                            <td>{{ result.originalValues.toAmountWithVisaRate }} {{ result.originalValues.toCurrency }}</td>
                        </tr>
                        <tr>
                            <th>Taux de change Visa</th>
                            <td>{{ result.originalValues.fxRateVisa }}</td>
                        </tr>
                        <tr>
                            <th>Date</th>
                            <td>{{ result.disclaimerDate ?? 'N/A' }}</td>
                        </tr>
                        <tr>
                            <th>Status</th>
                            <td>{{ result.status ?? 'N/A' }}</td>
                        </tr>
                    </table>
                {% endif %}
                
                <h4>📊 Données complètes:</h4>
                <pre>{{ result | json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
            </div>
        {% else %}
            <div class="error">
                <h3>❌ Erreur</h3>
                <p><strong>Message:</strong> {{ error }}</p>
            </div>
        {% endif %}
        
        <p><a href="{{ path('visa_test') }}">← Retour au test</a></p>
    </div>
</body>
</html>
